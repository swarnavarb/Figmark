{
  "name": "figmark",
  "version": "0.1.0",
  "private": true,
  "description": "Import-reseller marketplace: lot/manifest seller ops, two-sided verified reviews, escrow.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix api ci && npm --prefix app ci",
    "build": "npm run build:api && npm run build:app",
    "build:api": "npm --prefix api run build",
    "build:app": "npm --prefix app run build",
    "typecheck": "npm --prefix api run typecheck && npm --prefix app run typecheck",
    "test": "node scripts/smoke-auth.mjs && node scripts/smoke-api.mjs",
    "dev": "npm --prefix app run dev",
    "serve": "node scripts/dev-server.mjs",
    "azure:check": "node scripts/azure-check.mjs",
    "azure:provision": "node scripts/provision-cosmos.mjs"
  }
}
